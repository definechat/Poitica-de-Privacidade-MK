import React from 'react';

/**
 * @deprecated Este componente não é mais utilizado para renderizar a página.
 * O seu conteúdo foi convertido para HTML estático e movido para o arquivo index.html,
 * o que permite a publicação direta em serviços de hospedagem estática como a Netlify.
 * Para fazer alterações no conteúdo da página, edite diretamente o arquivo index.html.
 */
const PrivacyPolicy: React.FC = () => null;

export default PrivacyPolicy;
